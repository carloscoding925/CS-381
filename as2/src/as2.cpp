#include <raylib-cpp.hpp>

int main() {
    raylib::Window window(800, 400, "CS381 - Assignment 2");

    while(!window.ShouldClose()) {
        window.BeginDrawing();
        {
            
        }
        window.EndDrawing();
    }

    return 0;
}